{
  "dependencies": {
    "mongoose": "^6.10.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  },
  "scripts":{
    "predeploy":"npm run build",
    "deploy":"gh-pages -d build",
    "build":"react-scripts built",
    "test":"react-scripts built",
    "eject":"react-scripts eject"
}
}
